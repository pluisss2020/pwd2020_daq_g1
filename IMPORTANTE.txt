Hola profe, ¿cómo está?

El index aún no está programado ya que el index va a ser el login para acceso a la página.
Puede encontrar el archivo de la pagina principal en la carpeta subpaginas/index.php.

El archivo está en formato PHP ya que tendrá que tener verificación de permisos debido al Login
que se hará pronto.

Esperamos que le guste la interfaz grafica de nuestro sitio web, en estos días vamos a subir
la página con muchas grandes actualizaciones, incluyendo la base de datos y una exportacion de
los datos a un archivo excel.

Dirección para navegador: localhost/PWD/subpaginas/index.php